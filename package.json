{
  "name": "icons",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:icons": "node ./scripts/build.js && prettier --write ./src",
    "preview": "vite preview",
    "format": "prettier --write ./src",
    "lint": "eslint ./src",
    "test": "vitest",
    "test:coverage": "vitest --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^3.0.8",
    "eslint": "^9.22.0",
    "eta": "^3.5.0",
    "prettier": "^3.5.3"
  }
}
