{
  "name": "icons",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:icons": "node ./scripts/build.js && npm run format",
    "preview": "vite preview",
    "format": "prettier --write ./",
    "lint": "eslint ./src"
  },
  "devDependencies": {
    "eslint": "^9.22.0",
    "eta": "^3.5.0",
    "prettier": "^3.5.3"
  }
}
